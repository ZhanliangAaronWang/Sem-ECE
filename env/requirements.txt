# Core
numpy>=1.24
scipy>=1.10
scikit-learn>=1.3
matplotlib>=3.7

# Provider SDKs (each generation/clustering stage needs only its own)
openai>=1.40           # OpenAI + any OpenAI-compatible base_url (xAI, QUBRID/Kimi)
anthropic>=0.34
google-genai>=0.3
mistralai>=1.0

# Optional
xlsxwriter>=3.1        # audit sheet generation
